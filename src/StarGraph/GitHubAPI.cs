using System;
using System.Collections.Generic;
using System.Text;

namespace StarGraph
{
    public static class GitHubAPI
    {
        public static (string[] recentGazers, int stars) GetStarInfo(string user, string repo, string json)
        {
            return (null, 123);
        }
    }
}
